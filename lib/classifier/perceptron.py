import numpy as np
from lib.classifier.classifier import Classifier

# TODO
class Perceptron(Classifier):

    def __init__(self):
        super().__init__(proceptron_training, perceptron_classifying)


def proceptron_training(data, training_args):
    training_args[0]


def perceptron_classifying(data, model, classifying_args):
    return np.multiply(data, model)